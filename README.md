<div align="center">

# 🛠️ Post-Install Setup Script

![Bash](https://img.shields.io/badge/Language-Bash-4EAA25?style=for-the-badge&logo=gnu-bash&logoColor=white)
![Linux](https://img.shields.io/badge/Linux-FCC624?style=for-the-badge&logo=linux&logoColor=black)
![Arch](https://img.shields.io/badge/Arch_Linux-1793D1?style=for-the-badge&logo=arch-linux&logoColor=white)
![Debian](https://img.shields.io/badge/Debian-A81D33?style=for-the-badge&logo=debian&logoColor=white)

**Автоматизований скрипт для налаштування системи після встановлення.** *Підтримує Arch-based та Debian-based дистрибутиви.*

</div>

---

## ✨ Можливості

Цей скрипт проводить користувача через інтерактивне меню для налаштування системи:

* 🐧 **Автовизначення системи:** Працює з `pacman` (Arch) та `apt` (Debian/Ubuntu).
* 📦 **Масове встановлення:** Читає списки пакетів з файлів `pkglist*.txt`.
* 🎨 **Відновлення KDE Plasma:** Автоматично імпортує теми через `konsave`.
* ⌨️ **SDDM NumLock:** Вмикає цифрову клавіатуру на екрані входу.
* 🚀 **Zsh Environment:** Встановлює `Oh My Zsh`, `Powerlevel10k` та плагіни.
* 🔋 **Оптимізація:** Налаштовує `TLP` для збереження заряду ноутбука.

---

## 🚀 Як користуватися

### 1. Підготовка
Завантажте скрипт та покладіть необхідні файли в одну папку (див. структуру нижче).

### 2. Запуск
Відкрийте термінал у папці зі скриптом та виконайте команди:

```bash
# Надання прав на виконання
chmod +x install.sh

# Запуск (не використовуйте sudo, скрипт попросить його сам)
./install.sh
```
---

## ⚠️ Важливі попередження

> [!IMPORTANT]
> **Можливі переривання роботи скрипта**
>
> Під час виконання певних кроків скрипт може зупинитися або завершити поточну сесію користувача. Будь ласка, прочитайте це перед запуском.

### 🔹 Крок 3: SDDM (Екран входу)
Скрипт запропонує перезапустити дисплейний менеджер SDDM для застосування налаштувань NumLock. **Якщо ви погодитеся:**
1.  Графічна сесія **негайно завершиться**.
2.  Вас викине на екран логіна.
3.  Робота скрипта буде перервана.

> [!TIP]
> **💡 Рішення:**
> Увійдіть у систему знову, запустіть `install.sh` ще раз та **пропускайте** (`n`) ті кроки, які вже були успішно виконані (наприклад, встановлення пакетів).

### 🔹 Крок 4: Zsh (Оболонка)
Інсталятор *Oh My Zsh* часто автоматично запускає нову оболонку `zsh` прямо всередині поточного скрипта. Це виглядає так, ніби скрипт "завис" або зупинився.

> [!TIP]
> **💡 Рішення:**
> Якщо після встановлення Zsh скрипт зупинився і змінився вигляд командного рядка — просто введіть команду `exit` та натисніть **Enter**. Це поверне керування основному скрипту.

---

## 📂 Структура файлів

Для коректної роботи переконайтеся, що ваша робоча папка виглядає наступним чином:

```text
.
├── install.sh          # Головний скрипт (надайте права +x)
├── pkglist.txt         # Список пакетів для встановлення (по одному в рядок)
└── my_theme.knsv       # (Опціонально) Файл експорту Konsave для теми KDE
